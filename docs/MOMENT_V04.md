# Bashformer v0.4.0 — moment of consequence

Date: 2026-08-18

Weights used:

```text
bashformer-trained1000.bfw
SHA-256 1879971b7693a2e4b6988b4c1724d60a619d8758c93366b47d4f45c287b0c510
```

These are the 20,640-parameter Q12 weights from the earlier 1000-step
moment-of-truth run. That earlier training used a temporary Notorch-ABI-compatible
autograd backend because this container cannot resolve GitHub. It proved the
training/export/Bash-inference path, but it is not relabelled here as an upstream
Notorch run. v0.4 exercises the finished field dynamics on that learned body.

## Laws alter a learned distribution

Command family:

```bash
./bashformer.sh --weights bashformer-trained1000.bfw \
  --prompt 'The blood was ' --tokens 24 --generated-only \
  --temperature 0.7 --seed 42 ...
```

Outputs:

```text
vanilla:
warm. The night was stin

entropy floor = 1, ceiling = 1:
warm.
The bled.

CHAPTER

entropy floor = 0, ceiling = 0.2:
sthe nort the thaas d th

entropy floor = 0.8, ceiling = 0.4:
warm. The night, the bet
```

The laws therefore operate on the learned logits; they are not parser-only
metadata.

## Debt crosses its own threshold

A natural high-temperature run began at `VELOCITY RUN` with zero seeded debt:

```bash
./bashformer.sh --weights bashformer-trained1000.bfw \
  --prompt 'The blood was ' --tokens 48 --generated-only \
  --temperature 1.5 --seed 1 --method --velocity run --trace
```

Generated bytes:

```text
tjTime.was goy lome M" ra onend mome cra stranve
```

Recovery window:

```text
TRACE debt chosen=72 add=3146 total=19041 velocity=run    temp=7373 recovered=0 step=12 recoveries=0
TRACE debt chosen=80 add=996  total=19998 velocity=run    temp=7373 recovered=0 step=13 recoveries=0
TRACE debt chosen=90 add=3415 total=23367 velocity=nomove temp=3072 recovered=1 step=14 recoveries=1
TRACE debt chosen=1  add=0    total=23321 velocity=nomove temp=3072 recovered=0 step=15 recoveries=1
TRACE debt chosen=77 add=2514 total=25785 velocity=nomove temp=3072 recovered=0 step=16 recoveries=1
```

Q12 interpretation:

```text
base temperature = 1.5      -> 6144
RUN               = 1.2x    -> 7373
NOMOVE            = 0.5x    -> 3072
recovery debt      = 23367/Q -> about 5.70
```

The current token was sampled while running. The threshold crossing then forced
`NOMOVE`; the following token observed the lower temperature.

The independent C oracle produced the exact same 48 output bytes and the exact
same complete trace.

## The cooled field survives process death

A separate persistent experiment enabled BFSOMA2 and Hebbian memory.

Session 1:

```text
Cop he. tol the dove theahere the bear. Theas Th
```

Its recovery occurred at field step 11:

```text
TRACE debt chosen=85 add=3552 total=17171 velocity=run    temp=7373 recovered=0 step=9  recoveries=0
TRACE debt chosen=80 add=3157 total=20288 velocity=run    temp=7373 recovered=0 step=10 recoveries=0
TRACE debt chosen=77 add=2381 total=22625 velocity=nomove temp=3072 recovered=1 step=11 recoveries=1
TRACE debt chosen=1  add=0    total=22581 velocity=nomove temp=3072 recovered=0 step=12 recoveries=1
```

A new process loaded that state for Session 2. Its initial trace already read:

```text
TRACE method velocity=nomove base_temp=4915 temp=2458 ...
  debt=33961 ... edges=189 steps=48 recoveries=1 soma=2
```

Session 2 output:

```text
tet he the the sa sthe V
 te die
```

After both processes, BFSOMA2 held:

```text
M DEBT_Q 38573
M DEBT_DECAY_Q 4088
M ENTROPY_FLOOR_Q 410
M RESONANCE_CEILING_Q 3891
M VELOCITY nomove
M FIELD_STEPS 80
M RECOVERIES 1
```

Bash and C serialized the entire soma byte-for-byte after each session.

## Release gates

The v0.4 release passed:

```text
vanilla stage parity                 PASS
stateless Method parity              PASS
five velocity/RNG modes              PASS
entropy floor / resonance ceiling    PASS
sampled prophecy debt                PASS
live RUN -> NOMOVE recovery          PASS
next-token temperature feedback      PASS
Hebbian H-term                       PASS
BFSOMA2 persistence                  PASS
BFSOMA1 -> BFSOMA2 migration         PASS
read-only / FIELD OFF non-mutation   PASS
wide Q12 edge-count parser           PASS
malicious/duplicate/incomplete soma  PASS
empty-PATH runtime                   PASS
GCC                                  PASS
Clang                                PASS
ASan                                 PASS
UBSan                                PASS
```

The remaining external acceptance gate is intentionally named, not blurred:
run `make bootstrap && make doctor && make upstream-smoke` against the pinned
real Notorch checkout on a host with source access, then perform the full Dracula
training run.
